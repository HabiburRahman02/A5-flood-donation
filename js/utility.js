// input field value
function getInputFieldValueById(id) {
    const fieldValue = document.getElementById(id);
    return fieldValue;
}


// text field value
function getTextFieldValueById(id) {
    const textFieldValue = document.getElementById(id);
    return textFieldValue;
}